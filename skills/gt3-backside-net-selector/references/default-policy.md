# GT3 Backside Net Selector Default Policy

1. Power is the primary objective.
2. Timing is a veto or guard, not the primary reward.
3. Use clocks only if explicitly requested.
4. Emit both raw net names and Tcl-glob-escaped patterns.
5. Keep the selector reusable and independent from route submission scripts.
